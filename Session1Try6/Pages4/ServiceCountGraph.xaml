<Page x:Class="Session1Try6.Pages4.ServiceCountGraph"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Session1Try6.Pages4"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"

      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ServiceCountGraph">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal">
            <Label Content="С"/>
            <DatePicker Name="StartDp" SelectedDateChanged="StartDp_SelectedDateChanged"/>
            <Label Content="по"/>
            <DatePicker Name="EndDp" SelectedDateChanged="EndDp_SelectedDateChanged"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Column="2">
            <Button x:Name="TableBtn" Click="TableBtn_Click" Content="Таблица" />
            <Button x:Name="GraphBtn" Click="GraphBtn_Click" Content="График" IsEnabled="False"/>
        </StackPanel>

        <lvc:CartesianChart Margin="20" Name="StolbGraph" Grid.Row="1" Grid.ColumnSpan="3"/>
        
        <DataGrid x:Name="DgGraph" IsReadOnly="True" AutoGenerateColumns="False" Visibility="Collapsed" Grid.Row="1" Grid.ColumnSpan="3">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat=d}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
